server {
    listen 8080;

    location / {
        root www/default;
        index index.html;
        allow_methods GET;
    }

    location /uploads {
        root www/upload;
        allow_methods POST GET DELETE;
        upload_store www/upload;
        autoindex on;
    }
}
